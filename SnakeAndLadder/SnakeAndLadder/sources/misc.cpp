//t = SDL_CreateTexture(render, SDL_PIXELFORMAT_RGBA8888, SDL_TEXTUREACCESS_TARGET, 600, 300);	

		//Line

		//SDL_SetRenderDrawColor(render, 0x00, 0x00, 0x00, 0x00);
		//SDL_RenderClear(render);
		//SDL_SetRenderDrawColor(render, 0x00, 0xFF, 0x00, 0x00);
		//SDL_RenderDrawLine(render, 150, 150, 50, 50);
		//SDL_SetRenderTarget(render, NULL);
		//SDL_RenderPresent(render);

/*
int randNum(int min, int max) {
	std::random_device rd;
	std::mt19937 eng(rd());
	std::uniform_int_distribution<> distr(min, max);
	return distr(eng);
}*/

//Movable Rectangle

//SDL_SetRenderDrawColor(render, 0x00, 0x00, 0x00, 0x00);
//SDL_SetRenderTarget(render, t);
//SDL_RenderClear(render);
//SDL_SetRenderDrawColor(render, 0xFF, 0x00, 0x00, 0x00);
//SDL_RenderDrawRect(render, &rec);
//SDL_SetRenderDrawColor(render, 0x00, 0xFF, 0x00, 0x00);
//SDL_RenderFillRect(render, &rec);
//SDL_SetRenderTarget(render, NULL);
//SDL_RenderCopy(render, t, NULL, &rec);
//SDL_RenderPresent(render);

		/*case SDL_KEYDOWN:
			if(event.key.keysym.sym == SDLK_LEFT) {
				std::cout << "Left\n";
				h.rec.x -= 1;
			}
			else if (event.key.keysym.sym == SDLK_RIGHT) {
				std::cout << "Right\n";
				rec.x += 1;
			}
			else if (event.key.keysym.sym == SDLK_UP) {
				std::cout << "Up\n";
				rec.y -= 1;
			}
			else if (event.key.keysym.sym == SDLK_DOWN) {
				std::cout << "Down\n";
				rec.y += 1;
			}
			break;
		}*/